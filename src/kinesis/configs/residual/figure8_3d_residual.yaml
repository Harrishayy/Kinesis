# Figure-8 trajectory evaluated under the residual-RL env. Same residual FF /
# obs layout / wrappers as viviani_residual so a policy trained on Viviani can
# be loaded against this config zero-shot.

name: figure8_3d_residual

env:
  control_hz: 50
  max_steps: 500
  max_delta_rad: 0.0873
  reset_noise_rad: 0.02
  start_at_target: true
  tip_offset_m: 0.1034
  include_cartesian_velocities: true
  lookahead_n: 8
  lookahead_dt_s: 0.15
  home_qpos: [0.257813, 0.163813, -0.335467, -1.865636, 0.389176, 1.429329, -0.965127]

trajectory:
  kind: figure8_3d
  period_s: 4.0
  center_xyz: [0.5, 0.0, 0.4]
  amp_x_m: 0.10
  amp_y_m: 0.15
  amp_z_m: 0.10

wrappers:
  obs_noise_sigma_m: 0.02
  action_delay_steps: 2

reward:
  w_track: 10.0
  w_action_rate: 0.1
  w_qdot: 0.001
  w_inband: 0.5

residual_ff:
  enabled: true
  p_gain: 0.5
  damping: 0.01
  clip: 0.8

ppo:
  total_timesteps: 4_000_000
  n_envs: 16
  learning_rate: 3.0e-4
  n_steps: 2048
  batch_size: 64
  n_epochs: 10
  gamma: 0.99
  gae_lambda: 0.95
  net_arch: [256, 256]
